const { allTodosgetService } = require('../allTodos.service.js');
const { makeApiCall } = require('../../utils/index.js');
jest.mock('../../utils/index.js');
describe('allTodosgetService', () => {
  it('should pass the test', () => {
    //TODO
  });
});
